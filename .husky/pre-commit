yarn lint:fix
yarn prettier:fix

# Check Prettier standards
yarn prettier || {
    echo 'Prettier check failed. Run yarn prettier:fix'
    false;
}

# Check ESLint standards
yarn lint || {
    echo 'ESLint check failed. Fix errors and commit again.'
    false;
}

echo '✅ Code looks good. Committing...'